north, taxi_x, increment
	0******, increment
south, passg_x, set-to
	00**0**, set-to
	0001001, set-to
south, passg_y, increment
	00**0*0, increment
south, passg_y, set-to
	00**0**, set-to
south, in_taxi, increment
	00**0**, increment
south, in_taxi, set-to
	00**0*0, set-to
east, taxi_x, increment
	0*0**0*, increment
east, taxi_x, set-to
	0*00*0*, set-to
east, taxi_y, increment
	0*0**0*, increment
east, taxi_y, set-to
	010**0*, set-to
east, dest_x, increment
	0*0**0*, increment
east, dest_x, set-to
	0*0**0*, set-to
east, dest_y, increment
	0*0**0*, increment
east, dest_y, set-to
	0*0**0*, set-to
east, passg_x, increment
	010**00, increment
east, passg_x, set-to
	0*0**0*, set-to
east, passg_y, increment
	0*0**0*, increment
east, passg_y, set-to
	010**0*, set-to
east, in_taxi, increment
	0*0**0*, increment
east, in_taxi, set-to
	010**00, set-to
west, taxi_x, increment
	0**0*0*, increment
west, taxi_x, set-to
	0110000, set-to
west, taxi_y, increment
	0**0*0*, increment
west, taxi_y, set-to
	01*0*0*, set-to
west, dest_x, increment
	0**0*0*, increment
west, dest_x, set-to
	0**0*0*, set-to
west, dest_y, increment
	0**0*0*, increment
west, dest_y, set-to
	0**0*0*, set-to
west, passg_x, increment
	01*0*00, increment
west, passg_y, increment
	0**0*0*, increment
west, passg_y, set-to
	01*0*0*, set-to
west, in_taxi, increment
	0**0*0*, increment
west, in_taxi, set-to
	01*0*00, set-to
pickup, taxi_x, increment
	0100100, increment
pickup, taxi_x, set-to
	0100100, set-to
pickup, taxi_y, increment
	0100100, increment
pickup, taxi_y, set-to
	0100100, set-to
pickup, dest_x, increment
	0100100, increment
pickup, dest_x, set-to
	0100100, set-to
pickup, dest_y, increment
	0100100, increment
pickup, dest_y, set-to
	0100100, set-to
pickup, passg_x, increment
	0100100, increment
pickup, passg_x, set-to
	0100100, set-to
pickup, passg_y, increment
	0100100, increment
pickup, passg_y, set-to
	0100100, set-to
pickup, in_taxi, increment
	0100100, increment
pickup, in_taxi, set-to
	0100100, set-to
dropoff, taxi_x, increment
dropoff, taxi_x, set-to
dropoff, taxi_y, increment
dropoff, taxi_y, set-to
dropoff, dest_x, increment
dropoff, dest_x, set-to
dropoff, dest_y, increment
dropoff, dest_y, set-to
dropoff, passg_x, increment
dropoff, passg_x, set-to
dropoff, passg_y, increment
dropoff, passg_y, set-to
dropoff, in_taxi, increment
dropoff, in_taxi, set-to
Failure list Fa
north : 
south : 010100101000010101000011000001000000100100
east : 0110000
west : 00010010101001
pickup : 0001001000000101010010100001
dropoff : 0000001000100101000010101001
