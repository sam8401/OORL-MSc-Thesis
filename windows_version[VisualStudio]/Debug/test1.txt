north, taxi_x, increment
	00**000, increment
north, taxi_x, set-to
	0010000, set-to
north, taxi_y, increment
	00**000, increment
south, taxi_x, set-to
	*010000, set-to
	1000000, set-to
south, taxi_y, increment
	*0**000, increment
south, taxi_y, set-to
	10*0000, set-to
south, dest_x, increment
	*0**000, increment
south, dest_x, set-to
	*0**000, set-to
south, dest_y, increment
	*0**000, increment
south, dest_y, set-to
	*0**000, set-to
south, passg_x, increment
	*0**000, increment
south, passg_x, set-to
	*0**000, set-to
south, passg_y, increment
	*0**000, increment
south, passg_y, set-to
	*0**000, set-to
south, in_taxi, increment
	*0**000, increment
south, in_taxi, set-to
	*0**000, set-to
east, taxi_x, increment
	1000000, increment
east, taxi_y, increment
	1000000, increment
east, taxi_y, set-to
	1000000, set-to
east, dest_x, increment
	1000000, increment
east, dest_x, set-to
	1000000, set-to
east, dest_y, increment
	1000000, increment
east, dest_y, set-to
	1000000, set-to
east, passg_x, increment
	1000000, increment
east, passg_x, set-to
	1000000, set-to
east, passg_y, increment
	1000000, increment
east, passg_y, set-to
	1000000, set-to
east, in_taxi, increment
	1000000, increment
east, in_taxi, set-to
	1000000, set-to
west, taxi_x, increment
	*0*0000, increment
west, taxi_x, set-to
	*010000, set-to
west, taxi_y, increment
	*0*0000, increment
west, taxi_y, set-to
	10*0000, set-to
west, dest_x, increment
	*0*0000, increment
west, dest_x, set-to
	*0*0000, set-to
west, dest_y, increment
	*0*0000, increment
west, dest_y, set-to
	*0*0000, set-to
west, passg_x, increment
	*0*0000, increment
west, passg_x, set-to
	*0*0000, set-to
west, passg_y, increment
	*0*0000, increment
west, passg_y, set-to
	*0*0000, set-to
west, in_taxi, increment
	*0*0000, increment
west, in_taxi, set-to
	*0*0000, set-to
pickup, taxi_x, increment
pickup, taxi_x, set-to
pickup, taxi_y, increment
pickup, taxi_y, set-to
pickup, dest_x, increment
pickup, dest_x, set-to
pickup, dest_y, increment
pickup, dest_y, set-to
pickup, passg_x, increment
pickup, passg_x, set-to
pickup, passg_y, increment
pickup, passg_y, set-to
pickup, in_taxi, increment
pickup, in_taxi, set-to
dropoff, taxi_x, increment
dropoff, taxi_x, set-to
dropoff, taxi_y, increment
dropoff, taxi_y, set-to
dropoff, dest_x, increment
dropoff, dest_x, set-to
dropoff, dest_y, increment
dropoff, dest_y, set-to
dropoff, passg_x, increment
dropoff, passg_x, set-to
dropoff, passg_y, increment
dropoff, passg_y, set-to
dropoff, in_taxi, increment
dropoff, in_taxi, set-to
Failure list Fa
north : 10000001010000
south : 
east : 00100001010000
west : 
pickup : 
dropoff : 
